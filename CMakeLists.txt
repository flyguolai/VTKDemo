CMAKE_MINIMUM_REQUIRED(VERSION 3.7.1)
PROJECT( reslice)

FIND_PACKAGE( VTK REQUIRED )
INCLUDE( ${VTK_USE_FILE} )

ADD_EXECUTABLE( reslice reslice.cpp)
TARGET_LINK_LIBRARIES( reslice ${VTK_LIBRARIES} )